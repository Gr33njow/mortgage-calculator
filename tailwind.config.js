module.exports = {
    content: [
        "./*.html", // Scanne tous les fichiers HTML à la racine (ex. index.html)
    "./src/**/*.{html,js}"
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }